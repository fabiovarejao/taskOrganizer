# Example .env (copy to .env and fill values, DO NOT commit .env to VCS)
# Strong SA password for SQL Server (must meet SQL Server complexity rules)
SA_PASSWORD=saIron74Ds!#

# Connection string used by the API to connect to the sqlserver service
# Use the internal Docker service hostname (sqlserver) so the API container reaches the DB
CONNECTION_STRING=Server=sqlserver,1433;Database=TaskOrganizerDb;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True;MultipleActiveResultSets=true

# Whether the API should apply EF Core migrations at startup (true/false)
APPLY_MIGRATIONS=true
